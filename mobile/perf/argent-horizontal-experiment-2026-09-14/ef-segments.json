{
  "content": [
    {
      "type": "text",
      "text": "\"_Component `Segments` not found in this profiling session — no exact match, and no component whose displayed name is `Segments` once `Memo(...)` / `ForwardRef(...)` / `Forget(...)` wrappers are stripped. The session recorded 598 fiber renders across 14 commits._\\n\\nTo list every component in a commit instead, run `profiler-commit-query mode=by_index commit_index=<n>`.\""
    }
  ]
}

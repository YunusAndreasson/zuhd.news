diff --git a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ExpoUIModule.kt b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
index f7f0d1b..133dc12 100644
--- a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
+++ b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ExpoUIModule.kt
@@ -219,9 +219,13 @@ class ExpoUIModule : Module() {
       val expand by AsyncFunction()
       val partialExpand by AsyncFunction()
       val onDismissRequest by Event<Unit>()
+      val onBackPress by Event<Unit>()
 
       Content { props ->
-        ModalBottomSheetContent(props, hide, expand, partialExpand) { onDismissRequest(Unit) }
+        ModalBottomSheetContent(props, hide, expand, partialExpand,
+          onDismissRequest = { onDismissRequest(Unit) },
+          onBackPress = { onBackPress(Unit) }
+        )
       }
     }
 
diff --git a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
index e90edd3..71d1b24 100644
--- a/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
+++ b/node_modules/@expo/ui/android/src/main/java/expo/modules/ui/ModalBottomSheetView.kt
@@ -8,6 +8,8 @@ import android.content.Context
 import android.graphics.Color
 import android.view.KeyEvent
 import android.view.inputmethod.InputMethodManager
+import androidx.activity.ComponentDialog
+import androidx.activity.OnBackPressedCallback
 import androidx.compose.material3.BottomSheetDefaults
 import androidx.compose.material3.ExperimentalMaterial3Api
 import androidx.compose.material3.ModalBottomSheet
@@ -18,6 +20,7 @@ import androidx.compose.runtime.Composable
 import androidx.compose.runtime.DisposableEffect
 import androidx.compose.runtime.LaunchedEffect
 import androidx.compose.runtime.rememberCoroutineScope
+import androidx.compose.runtime.rememberUpdatedState
 import androidx.compose.ui.platform.LocalView
 import androidx.compose.ui.window.DialogWindowProvider
 import expo.modules.kotlin.records.Field
@@ -38,6 +41,7 @@ data class ModalBottomSheetPropertiesRecord(
 
 @OptimizedComposeProps
 data class ModalBottomSheetViewProps(
+  val interceptBackPress: Boolean = false,
   val skipPartiallyExpanded: Boolean = false,
   val initialFullyExpanded: Boolean = false,
   val containerColor: Color? = null,
@@ -70,14 +74,27 @@ private fun Activity.isAcceptingText(): Boolean {
  * Without this check, typing "rr" in a sheet TextInput reloads the app.
  */
 @Composable
-private fun ForwardKeyEventsToActivity(activity: Activity?) {
+private fun ForwardKeyEventsToActivity(activity: Activity?, interceptBackPress: Boolean, onBackPress: () -> Unit) {
   val dialog = (LocalView.current.parent as? DialogWindowProvider)?.window?.callback as? Dialog
   if (dialog == null || activity == null) {
     return
   }
 
-  DisposableEffect(dialog, activity) {
+  val currentOnBackPress = rememberUpdatedState(onBackPress)
+  DisposableEffect(dialog, activity, interceptBackPress) {
+    val backCallback = object : OnBackPressedCallback(interceptBackPress) {
+      override fun handleOnBackPressed() {
+        currentOnBackPress.value()
+      }
+    }
+    (dialog as? ComponentDialog)?.onBackPressedDispatcher?.addCallback(backCallback)
     dialog.setOnKeyListener { _, keyCode, event ->
+      if (interceptBackPress && keyCode == KeyEvent.KEYCODE_BACK) {
+        if (event.action == KeyEvent.ACTION_UP && !event.isCanceled) {
+          currentOnBackPress.value()
+        }
+        return@setOnKeyListener true
+      }
       val isForwardable = event.action == KeyEvent.ACTION_UP &&
         keyCode != KeyEvent.KEYCODE_BACK &&
         keyCode != KeyEvent.KEYCODE_ESCAPE &&
@@ -85,6 +102,7 @@ private fun ForwardKeyEventsToActivity(activity: Activity?) {
       isForwardable && activity.onKeyUp(keyCode, event)
     }
     onDispose {
+      backCallback.remove()
       dialog.setOnKeyListener(null)
     }
   }
@@ -96,7 +114,8 @@ fun FunctionalComposableScope.ModalBottomSheetContent(
   hide: AsyncFunctionHandle<Unit>,
   expand: AsyncFunctionHandle<Unit>,
   partialExpand: AsyncFunctionHandle<Unit>,
-  onDismissRequest: () -> Unit
+  onDismissRequest: () -> Unit,
+  onBackPress: () -> Unit
 ) {
   val sheetState = rememberModalBottomSheetState(props.skipPartiallyExpanded)
   val scope = rememberCoroutineScope()
@@ -156,12 +175,12 @@ fun FunctionalComposableScope.ModalBottomSheetContent(
       else -> null
     },
     properties = ModalBottomSheetProperties(
-      shouldDismissOnBackPress = props.properties.shouldDismissOnBackPress,
+      shouldDismissOnBackPress = props.properties.shouldDismissOnBackPress && !props.interceptBackPress,
       shouldDismissOnClickOutside = props.properties.shouldDismissOnClickOutside
     ),
     modifier = ModifierRegistry.applyModifiers(props.modifiers, appContext, composableScope, globalEventDispatcher)
   ) {
-    ForwardKeyEventsToActivity(appContext.currentActivity)
+    ForwardKeyEventsToActivity(appContext.currentActivity, props.interceptBackPress, onBackPress)
     Children(UIComposableScope(), filter = { !isSlotView(it) })
   }
 
diff --git a/node_modules/@expo/ui/build/community/bottom-sheet/types.d.ts b/node_modules/@expo/ui/build/community/bottom-sheet/types.d.ts
index 72fc6be..e84ae46 100644
--- a/node_modules/@expo/ui/build/community/bottom-sheet/types.d.ts
+++ b/node_modules/@expo/ui/build/community/bottom-sheet/types.d.ts
@@ -112,6 +112,8 @@ export interface BottomSheetFooterProps {
  * a modal sheet overlay on iOS and Android. On web, it renders inline.
  */
 export interface BottomSheetProps {
+  /** Android: handle Back inside the sheet instead of dismissing it. */
+  onBackPress?: () => void;
     /**
      * Ref to the bottom sheet component. Exposes imperative methods (`snapToIndex`,
      * `expand`, `collapse`, `close`, `present`, `dismiss`, etc.).
diff --git a/node_modules/@expo/ui/build/jetpack-compose/ModalBottomSheet/index.d.ts b/node_modules/@expo/ui/build/jetpack-compose/ModalBottomSheet/index.d.ts
index ddf89aa..4af2320 100644
--- a/node_modules/@expo/ui/build/jetpack-compose/ModalBottomSheet/index.d.ts
+++ b/node_modules/@expo/ui/build/jetpack-compose/ModalBottomSheet/index.d.ts
@@ -30,6 +30,9 @@ export type ModalBottomSheetProperties = {
     shouldDismissOnClickOutside?: boolean;
 };
 export interface ModalBottomSheetProps {
+  /** Android: keep the sheet open and emit onBackPress. */
+  interceptBackPress?: boolean;
+  onBackPress?: () => void;
     /**
      * The children of the `ModalBottomSheet` component.
      * Can include a `ModalBottomSheet.DragHandle` slot for a custom drag handle.
diff --git a/node_modules/@expo/ui/src/community/bottom-sheet/BottomSheet.android.tsx b/node_modules/@expo/ui/src/community/bottom-sheet/BottomSheet.android.tsx
index ac8ae4a..74fe919 100644
--- a/node_modules/@expo/ui/src/community/bottom-sheet/BottomSheet.android.tsx
+++ b/node_modules/@expo/ui/src/community/bottom-sheet/BottomSheet.android.tsx
@@ -60,6 +60,7 @@ export function BottomSheet(props: BottomSheetProps) {
     onChange,
     onClose,
     onDismiss,
+    onBackPress,
     enablePanDownToClose = false,
     enableDynamicSizing = true,
     handleComponent,
@@ -190,6 +191,8 @@ export function BottomSheet(props: BottomSheetProps) {
           <ModalBottomSheet
             ref={sheetRef}
             onDismissRequest={handleDismiss}
+            interceptBackPress={!!onBackPress}
+            onBackPress={onBackPress}
             skipPartiallyExpanded={skipPartially}
             initialFullyExpanded={hasMultipleSnapPoints && pendingIndexRef.current === maxIndex}
             showDragHandle={handleComponent !== null}
diff --git a/node_modules/@expo/ui/src/community/bottom-sheet/types.ts b/node_modules/@expo/ui/src/community/bottom-sheet/types.ts
index d7372fc..3610b43 100644
--- a/node_modules/@expo/ui/src/community/bottom-sheet/types.ts
+++ b/node_modules/@expo/ui/src/community/bottom-sheet/types.ts
@@ -121,6 +121,8 @@ export interface BottomSheetFooterProps {
  * a modal sheet overlay on iOS and Android. On web, it renders inline.
  */
 export interface BottomSheetProps {
+  /** Android: handle Back inside the sheet instead of dismissing it. */
+  onBackPress?: () => void;
   /**
    * Ref to the bottom sheet component. Exposes imperative methods (`snapToIndex`,
    * `expand`, `collapse`, `close`, `present`, `dismiss`, etc.).
diff --git a/node_modules/@expo/ui/src/jetpack-compose/ModalBottomSheet/index.tsx b/node_modules/@expo/ui/src/jetpack-compose/ModalBottomSheet/index.tsx
index c3225b1..ae21e75 100644
--- a/node_modules/@expo/ui/src/jetpack-compose/ModalBottomSheet/index.tsx
+++ b/node_modules/@expo/ui/src/jetpack-compose/ModalBottomSheet/index.tsx
@@ -44,6 +44,9 @@ export type ModalBottomSheetProperties = {
 };
 
 export interface ModalBottomSheetProps {
+  /** Android: keep the sheet open and emit onBackPress. */
+  interceptBackPress?: boolean;
+  onBackPress?: () => void;
   /**
    * The children of the `ModalBottomSheet` component.
    * Can include a `ModalBottomSheet.DragHandle` slot for a custom drag handle.
